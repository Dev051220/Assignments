#include <stdio.h>  
int main() {  
    int side, area;  
printf("Enter the lenghth of the side of the cube: ");  
scanf("%d", &side);  
    area = 6 * side * side;    
printf("Area of the cube: %d\n", area);   
    return 1;  
}  